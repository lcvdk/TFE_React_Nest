const PlayerDetail = ({ player_id, player_pseudo, player_email }) => (
  <div>
      <p>{player_id}</p>
      <p>{player_pseudo}</p>
      <p>{player_email}</p>
  </div>
);

export default PlayerDetail;
